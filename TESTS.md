# Tests

Run all tests:

```bash
npm run test
```

## Automated tests

1. `tests/audit-engine.test.ts` — downgrades small Cursor Business teams to Pro.
2. `tests/audit-engine.test.ts` — flags ChatGPT Team as inefficient for two seats.
3. `tests/audit-engine.test.ts` — does not manufacture savings for optimal Copilot spend.
4. `tests/audit-engine.test.ts` — recommends credits for high API spend.
5. `tests/audit-engine.test.ts` — calculates annual savings as monthly savings × 12.
